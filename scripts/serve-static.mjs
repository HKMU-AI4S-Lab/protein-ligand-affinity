import http from 'node:http';
import {createReadStream} from 'node:fs';
import {stat} from 'node:fs/promises';
import {resolve,extname,sep} from 'node:path';
import {siteBase} from '../deployment.config.mjs';
const root=resolve('dist'),port=Number(process.argv[2]||4321),types={'.html':'text/html; charset=utf-8','.js':'text/javascript','.mjs':'text/javascript','.css':'text/css','.json':'application/json','.wasm':'application/wasm','.svg':'image/svg+xml','.png':'image/png','.webp':'image/webp','.jpg':'image/jpeg','.txt':'text/plain; charset=utf-8','.pdf':'application/pdf','.woff2':'font/woff2','.bin':'application/octet-stream','.onnx':'application/octet-stream'};
http.createServer(async(req,res)=>{try{const url=new URL(req.url,'http://local'),pathname=decodeURIComponent(url.pathname);if(siteBase!=='/'&&(pathname==='/'||pathname===siteBase.slice(0,-1))){res.writeHead(302,{Location:siteBase+url.search}).end();return;}if(!pathname.startsWith(siteBase)){res.writeHead(404).end('Not found');return;}let path=resolve(root,'.'+pathname.slice(siteBase.length-1));if(path!==root&&!path.startsWith(root+sep)){res.writeHead(403).end();return;}if((await stat(path)).isDirectory()){if(!pathname.endsWith('/')){res.writeHead(301,{Location:url.pathname+'/'+url.search}).end();return;}path=resolve(path,'index.html');}const info=await stat(path);res.writeHead(200,{'Content-Type':types[extname(path)]||'application/octet-stream','Content-Length':info.size,'Cache-Control':'no-cache'});if(req.method==='HEAD')res.end();else{const stream=createReadStream(path);stream.on('error',()=>res.destroy());res.on('close',()=>stream.destroy());stream.pipe(res);}}catch{res.writeHead(404).end('Not found');}}).listen(port,'127.0.0.1',()=>console.log(`Research preview: http://127.0.0.1:${port}${siteBase}`));
